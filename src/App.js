import React from 'react';
import TodoList from './Todolist'; // Adjust the path if TodoList.js is in a different directory

function App() {
  return (
    <div className="App">
      <TodoList />
    </div>
  );
}

export default App